-- CreateTable
CREATE TABLE "DoorSensor" (
    "id" INT4 NOT NULL GENERATED BY DEFAULT AS IDENTITY,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT "DoorSensor_pkey" PRIMARY KEY ("id")
);
